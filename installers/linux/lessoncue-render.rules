# Keep Intel render-node access available to LessonCue's unprivileged media
# worker. The service account is the only added owner; render nodes remain
# non-world-accessible.
SUBSYSTEM=="drm", KERNEL=="renderD*", ATTRS{vendor}=="0x8086", OWNER="lessoncue", MODE="0660"
